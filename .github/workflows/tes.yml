name: Verify Database
  run: |
    echo "Checking database..."
    npx prisma db execute --stdin <<< "
      SELECT 
        (SELECT COUNT(*) FROM \"gold_stores\") as stores;
    "
  env:
    DATABASE_URL: ${{ secrets.DATABASE_URL }}
